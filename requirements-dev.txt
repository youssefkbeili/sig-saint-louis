# Test-only dependencies — NOT required to run the application, NOT deployed to Vercel.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Run tests with: pytest tests/ -v
pytest>=8.0
httpx>=0.27
